/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://mini-custom-pc-showcase-git-main-deekshit7483s-projects.vercel.app'], // If you use remote images
  },
};

module.exports = nextConfig;